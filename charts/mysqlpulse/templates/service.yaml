{{- range .Values.targets }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mysqlpulse.targetFullname" (dict "root" $ "target" .) }}
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "mysqlpulse.labels" $ | nindent 4 }}
    app.kubernetes.io/component: {{ .name }}
spec:
  clusterIP: None
  selector:
    {{- include "mysqlpulse.selectorLabels" $ | nindent 4 }}
    app.kubernetes.io/component: {{ .name }}
  ports:
    - name: metrics
      port: {{ .metricsPort | default 9104 }}
      targetPort: metrics
      protocol: TCP
{{- end }}
